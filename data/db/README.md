# Local SQLite Databases

Runtime SQLite files live here by default. They are ignored by git; keep this README so the directory is easy to find in a fresh checkout.

Set `DB_DIR`, `FILL_DB_PATH`, or `TELEMETRY_DB_PATH` to override the defaults for a specific run.
